@model IEnumerable<SistemaAFT.Models.Estado_Civil>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre_Edo_Civil)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre_Edo_Civil)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Estado_CivilID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Estado_CivilID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Estado_CivilID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
